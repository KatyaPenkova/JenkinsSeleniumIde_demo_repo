pipeline{
    agent any

    stages{
        stage("Checkout code") {
        //checkout the repository
            steps{
                git branch: 'main', url: 'https://github.com/KatyaPenkova/JenkinsSeleniumIde_demo_repo'
            }
        }

        // stage("Setup .Net core") {
        // //checkout the repository
        //     steps{
        //         bat '''
        //         echo Downloading .Net 6 SDK
        //         curl 
        //     }
        // }
    }
}